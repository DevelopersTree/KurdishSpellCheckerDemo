<Window x:Class="SpellCheckerDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpellCheckerDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="5*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBox x:Name="txtWord" Margin="20, 10" TextChanged="txtWord_TextChanged"/>

        <ListBox x:Name="listBox" Grid.Row="1" />

        <TextBlock x:Name="ellapsed" Grid.Row="2" />
    </Grid>
</Window>
